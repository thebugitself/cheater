# smbclient

% smb, smbclient

## Interactive SMB client
#plateform/linux #target/remote #port/445 #protocol/smb #cat/ATTACK/CONNECT

```bash
smbclient //<IP>/<ShareName> -U <Username>
```

## Interactive SMB client (smbclient-ng)
#plateform/linux #target/remote #port/445 #protocol/smb #cat/ATTACK/CONNECT

```bash
smbclientng -u '<Username>' -p '<Password>' --host '<IP>'
```