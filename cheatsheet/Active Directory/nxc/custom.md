# custom

% smb, active directory, enumeration, exploitation

## nxc custom command
#plateform/linux #target/remote #port/445 #protocol/smb #cat/CUSTOM

```bash
nxc <protocol> <IP> -u <Username> <Creds_Options|-p|-H|-p -k> <Password> --timeout=10 <Custom_Parameter>
```